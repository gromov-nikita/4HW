package subjects;

public class English extends Subject {
    public English(String name, int time, String complexity) {
        setName(name);
        setTime(time);
        setComplexity(complexity);
    }
}
