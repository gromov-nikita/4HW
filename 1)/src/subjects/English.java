package subjects;

public class English extends Subject {
    public English(int time, String complexity) {
        setName("English");
        setTime(time);
        setComplexity(complexity);
    }
}
